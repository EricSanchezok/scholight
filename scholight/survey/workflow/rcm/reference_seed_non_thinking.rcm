name = "Scholight Survey Reference Seed Non-thinking Fallback"

model deepseek-v4-flash {
    protocol = "deepseek"
    endpoint = "https://api.deepseek.com"
    credentials = { env = "DEEPSEEK_API_KEY" }
    timeout = "1800"
    thinking = "false"
    limit = { context = "1000000", output = "393216" }
    modalities = { input = ["text"], output = ["text"] }
}

mcp scholight {
    transport = http
    url = "http://api:8000/mcp"
    headers = { Authorization = env "SCHOLIGHT_SURVEY_MCP_AUTHORIZATION" }
}

accelerator {
    purpose = "Scholight survey reference seed resolver fallback"
    models = ["deepseek-v4-flash"]
    policy = "captain"
    prompts = { captain = file "../prompts/reference_seed.txt" }
    tools = ["fs"]
    mcps = ["scholight"]
}
